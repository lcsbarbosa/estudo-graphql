type Cliente{
  id: ID,
  nome: String,
  email: String
}

type Query{
  cliente(id: ID): Cliente
  clientes: [Cliente]
}

type Mutation{
  saveClient(id: ID, nome: String, email: String): Cliente
  deleteClient(id: ID): Boolean
}