type Cliente{
  id: ID!,
  nome: String,
  email: String
}

input ClienteInput{
  id: ID,
  nome: String!,
  email: String!
}

extend type Query{
  client(id: ID!): Cliente
  clients: [Cliente]!
}

type Mutation{
  saveClient(client: ClienteInput): Cliente!
  deleteClient(id: ID!): Boolean!
}