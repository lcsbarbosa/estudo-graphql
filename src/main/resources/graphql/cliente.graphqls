type Cliente{
  id: ID,
  nome: String,
  email: String
}

type Query{
  client(id: ID): Cliente
  clients: [Cliente]
}

type Mutation{
  saveClient(id: ID, nome: String, email: String): Cliente
  deleteClient(id: ID): Boolean
}